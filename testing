import React, { useState } from "react";

type DocumentStatus = "Pending" | "Approved" | "Rejected";

interface CrewDocument {
  id: number;
  crewName: string;
  documentType: string;
  status: DocumentStatus;
  uploadDate: string;
}

const approvalConditions: Record<string, string> = {
  "LATEST PASSPORT": "Ensure both front and back are uploaded in PDF format and passport is valid for at least 6 months.",
  "CREW ID CARD": "Front and back must be clearly visible in PDF format. Must match crew details.",
  "TRANSFER LETTER": "Only applicable if crew has changed base or department. Must be signed by HR.",
  "INITIAL CREW CONTRACT": "Must be signed and dated. Verify joining date and role.",
  "RENEWAL CREW CONTRACT": "Check for updated terms and renewal period. Must be signed.",
  "PROMOTION LETTER": "Applicable only if crew has been promoted. Must be signed by authorized personnel.",
  "PHOTOGRAPH PASSPORT SIZE": "Should be clear with white or blue background. No shadows or filters.",
  "EDUCATION CERTIFICATE": "Verify certificates from 10th to Graduation. Must be legible and authentic.",
  "AAHAR CARD": "Ensure full name and address are visible. Must be in PDF format.",
  "PAN CARD": "Verify PAN number and name. Must be clear and match crew records.",
};

const documentTypes = Object.keys(approvalConditions);

const mockData: CrewDocument[] = Array.from({ length: 50 }, (_, i) => ({
  id: i + 1,
  crewName: `Crew Member ${i % 10 + 1}`,
  documentType: documentTypes[i % documentTypes.length],
  status: "Pending",
  uploadDate: `2025-09-${(i % 30) + 1}`,
}));

const CrewDocuments: React.FC = () => {
  const [expandedCrew, setExpandedCrew] = useState<string | null>(null);
  const [infoDocType, setInfoDocType] = useState<string | null>(null);
  const [approvedDocs, setApprovedDocs] = useState<number[]>([]);
  const [rejectedDocs, setRejectedDocs] = useState<number[]>([]);
  const [searchTerm, setSearchTerm] = useState("");
  const [selectedDocId, setSelectedDocId] = useState("");

  

  const crewList = Array.from(new Set(mockData.map((doc) => doc.crewName)));

  const getPendingDocs = (crewName: string) =>
    mockData.filter((doc) => doc.crewName === crewName && !approvedDocs.includes(doc.id) && !rejectedDocs.includes(doc.id));

  const totalPending = mockData.length - approvedDocs.length - rejectedDocs.length;

  const filteredCrewList = crewList.filter((name) =>
    name.toLowerCase().includes(searchTerm.toLowerCase())
  );

  return (
    <div className="p-6 bg-white rounded-lg shadow-md">
      {/* Top Cards */}
      <div className="mb-4 flex gap-4">
        <div className="bg-yellow-100 border-l-4 border-yellow-400 p-4 rounded w-1/3">
          <h3 className="text-lg font-semibold text-yellow-700">Pending Documents</h3>
          <p className="text-2xl font-bold text-yellow-800">{totalPending}</p>
        </div>
      </div>

      {/* Search Bar */}
      <div className="p-[3px] rounded-full bg-gradient-to-r from-yellow-400 via-red-500 via-green-400 to-blue-500 animate-glow">
    <input
      type="text"
      placeholder="Ask Anything with AI mode..."
      value={searchTerm}
      onChange={(e) => setSearchTerm(e.target.value)}
      className="w-full pr-32 pl-4 py-3 text-sm text-black bg-white rounded-full focus:outline-none"
    />
  </div>

      {/* Table */}
      <table className="w-full mt-3 table-auto rounded-md border border-[#000099]">
        <thead className="bg-[#e6f0ff] text-[#000099]">
          <tr>
            <th className="px-4 py-2 text-left">Crew Name</th>
            <th className="px-4 py-2 text-left">Upload Date</th>
            <th className="px-4 py-2 text-left">Documents</th>
            <th className="px-4 py-2 text-left">Status</th>
          </tr>
        </thead>
        <tbody>
          {filteredCrewList.map((crewName) => {
            const docs = getPendingDocs(crewName);
            const allApproved = docs.length === 0;

            return (
              <React.Fragment key={crewName}>
                <tr className="border-t">
                  <td className="px-4 py-2">{crewName}</td>
                  <td className="px-4 py-2">{docs[0]?.uploadDate || "-"}</td>
                  <td className="px-4 py-2">
                    <button
                      onClick={() =>
                        setExpandedCrew(expandedCrew === crewName ? null : crewName)
                      }
                      className="text-[#000099] underline"
                    >
                      {docs.length} Document{docs.length !== 1 ? "s" : ""}
                    </button>
                  </td>
                  <td className="px-4 py-2">
                    {allApproved ? (
                      <div className="flex items-center gap-2 text-green-600 font-semibold">
                        <svg
                          xmlns="http://www.w3.org/2000/svg"
                          className="h-5 w-5 text-green-600 transition-transform duration-500 transform scale-100"
                          fill="none"
                          viewBox="0 0 24 24"
                          stroke="currentColor"
                          strokeWidth={2}
                        >
                          <path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7" />
                        </svg>
                        Approved
                      </div>
                    ) : (
                      <span className="text-yellow-600 font-medium">Pending</span>
                    )}
                  </td>
                </tr>

                {/* Dropdown for documents */}
                {expandedCrew === crewName && (
                  <tr>
                    <td colSpan={4} className="bg-gray-50 border-t border-b">
                      <div
                      onClick={() => {
                    setSelectedPdfUrl(doc.pdfUrl);
                    setExtractedData({ type: doc.documentType, name: doc.name });
                    setSelectedDocId(doc.id); // ✅ Track which doc is selected
                    setShowPreview(true);
                  }}
              className="cursor-pointer min-w-[180px] flex flex-col items-center justify-between p-3 rounded border shadow-sm"
            >
      {/* PDF Icon */}
      <div className="text-red-600 mb-2">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          className="h-10 w-10"
          fill="currentColor"
          viewBox="0 0 24 24"
        >
          <path d="M6 2a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6H6zm7 1.5L18.5 9H13a1 1 0 0 1-1-1V3.5zM8 13h1v5H8v-5zm2.5 0h1.5a1.5 1.5 0 0 1 0 3H10.5v-3zm3.5 0h1v5h-1v-5z" />
        </svg>
      </div>

      {/* Document Name */}
      <span className="text-center font-medium text-sm mb-2">{doc.documentType}</span>
                      </div>
        {/* Actions */}
        <div className="flex items-center space-x-2">
          {approvedDocs.includes(doc.id) ? (
            <svg
              xmlns="http://www.w3.org/2000/svg"
              className="h-6 w-6 text-green-600"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              strokeWidth={2}
            >
              <path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7" />
            </svg>
          ) : rejectedDocs.includes(doc.id) ? (
            <span className="text-red-600 font-semibold text-sm">Rejected</span>
          ) : (
            <>
              <button
                onClick={() => setApprovedDocs((prev) => [...prev, doc.id])}
                className="px-2 py-1 bg-green-100 text-green-700 border border-green-500 rounded hover:bg-green-200 text-xs"
              >
                Approve
              </button>
              <button
                onClick={() => setRejectedDocs((prev) => [...prev, doc.id])}
                className="px-2 py-1 bg-red-100 text-red-700 border border-red-500 rounded hover:bg-red-200 text-xs"
              >
                Reject
              </button>
            </>
          )}
        </div>
      </div>
    ))}
  </div>
</td>

                  </tr>
                )}
              </React.Fragment>
            );
          })}
        </tbody>
      </table>

      {showPreview && (
  <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div className="bg-white rounded-lg shadow-lg w-[90%] max-w-6xl h-[80vh] flex overflow-hidden">
      
      {/* Left: PDF Viewer */}
      <div className="w-1/2 bg-gray-100 p-4 overflow-auto">
        <iframe
          src={selectedPdfUrl}
          title="PDF Preview"
          className="w-full h-full border rounded"
        />
      </div>

      {/* Right: Extracted Content */}
      <div className="w-1/2 p-6 relative flex flex-col justify-between">
        
        {/* ℹ️ Instructions Button */}
        <button
          onClick={() => setShowInstructions(true)}
          className="absolute top-4 right-4 text-[#000099] hover:text-blue-700"
          title="Instructions"
        >
          ℹ️
        </button>

        {/* Extracted Data */}
        <div>
          <h2 className="text-lg font-semibold mb-2">Extracted Data</h2>
          <p><strong>Type:</strong> {extractedData.type}</p>
          <p><strong>Name:</strong> {extractedData.name}</p>
        </div>

        {/* Comment Box */}
        <div className="mt-4">
          <label className="block text-sm font-medium mb-1">Comment</label>
          <textarea
            value={comment}
            onChange={(e) => setComment(e.target.value)}
            className="w-full border rounded p-2 h-24 resize-none"
            placeholder="Add your comment here..."
          />
        </div>

        {/* Action Buttons */}
        <div className="flex justify-end space-x-4 mt-4">
          <button
            onClick={() => {
              handleApprove(); // Define this function
              setShowPreview(false);
            }}
            className="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700"
          >
            Approve
          </button>
          <button
            onClick={() => {
              handleReject(); // Define this function
              setShowPreview(false);
            }}
            className="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700"
          >
            Reject
          </button>
        </div>
      </div>
    </div>
  </div>
)}

      {/* Info Modal */}
      {infoDocType && (
        <div className="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50">
          <div className="bg-white rounded-lg p-6 w-[90%] max-w-md shadow-lg">
            <h3 className="text-xl font-semibold text-[#000099] mb-2">
              Approval Conditions
            </h3>
            <p className="text-gray-700 mb-4">
              {approvalConditions[infoDocType] || "No specific conditions for this document."}
            </p>
            <div className="flex justify-end">
              <button
                onClick={() => setInfoDocType(null)}
                className="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400"
              >
                Close
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
};

export default CrewDocuments;
